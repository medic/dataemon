name: 'dataemon'
version: '0.0.1'
config-version: 2
profile: 'default'

require-dbt-version: [">=0.20.0", "<=1.0.0"]

config-version: 2

target-path: "target"
clean-targets: ["target", "dbt_modules"]
macro-paths: ["macros"]
log-path: "logs"
